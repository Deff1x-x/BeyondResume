# Server-only. Used by Next.js rewrites; never expose to the browser.
# Local default (also applied when unset): http://localhost:8000
# Docker Compose sets: http://backend:8000
API_UPSTREAM=http://localhost:8000
